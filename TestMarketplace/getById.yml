info:
  name: getById
  type: http

http:
  method: GET
  url: http://localhost:8080/api/produtos/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
